#include <bits/stdc++.h>

using namespace std;


int setBit(int N, int M, int i, int j){
	int max = ~0;
	int left = max - ((1<<j)-1);
	int right = (1 << i - 1);
	int  mask = right | left;
	return (n & mask) | (m << i);
	int  mask = right | left;
}

int main(){
int a,b,c,d;
	a=1;
	b=2;
	c=a+b;
	return a+b;
return 0;
}
